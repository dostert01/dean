# dean the server
A tiny and easy to use HTTP server for your C++ projects

Look an [example](./example) for how to use this server in your c++ prject.

## How to build

- clone the repo
```bash
cd dean
mkdir build
cd build
cmake -DCMAKE_POLICY_VERSION_MINIMUM=3.5   ..
make -j
```
